CONFIG_ZMK_KEYBOARD_NAME="roba"

# 右手(セントラル)と同期：スタックサイズの拡張
CONFIG_ZMK_BLE_THREAD_STACK_SIZE=4096
CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=4096
CONFIG_INPUT_THREAD_STACK_SIZE=4096

# 右手(セントラル)と同期：通信安定化設定
# 1M PHYに固定し、左右間の通信の安定性を確保
CONFIG_BT_CTLR_PHY_2M=n

# 送信出力を最大(+8dBm)に設定し、右手への到達性を高める
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Bluetoothスレッドの優先度を右手と合わせる
CONFIG_ZMK_BLE_THREAD_PRIORITY=3

# 基本機能
CONFIG_ZMK_MOUSE=y
CONFIG_ZMK_POINTING=y
CONFIG_NFCT_PINS_AS_GPIOS=y
CONFIG_ZMK_BLE=y

# バッテリー報告
CONFIG_ZMK_BATTERY_REPORTING=y

# ZMK Studio
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
